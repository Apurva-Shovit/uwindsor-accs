{
  "name": "acare-mvp-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix apps/web && npm install",
    "dev:web": "npm run dev --prefix apps/web",
    "dev:api": "powershell -Command \"cd apps/api; npx nodemon --watch app --ext py,env --exec '.\\venv\\Scripts\\python.exe -m uvicorn app.main:app --port 8000'\"",
    "dev:api:bash": "cd apps/api && npx nodemon --watch app --ext py,env --exec './venv/bin/python -m uvicorn app.main:app --port 8000'",
    "dev": "concurrently --kill-others \"npm run dev:api\" \"npm run dev:web\"",
    "dev:unix": "concurrently --kill-others \"npm run dev:api:bash\" \"npm run dev:web\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.14"
  }
}
