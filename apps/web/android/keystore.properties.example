# Copy to android/keystore.properties (gitignored) to sign release builds.
# storeFile is resolved relative to the android/app directory, so an absolute
# path outside the repo is the safest choice.
storeFile=C:/keys/acare-release.jks
storePassword=
keyAlias=acare
keyPassword=
